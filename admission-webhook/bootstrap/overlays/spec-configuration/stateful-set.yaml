apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: stateful-set
spec:
  template:
    spec:
      containers:
      - name: bootstrap
        resources:
          requests:
            cpu: 20m  
            memory: 100Mi
          limits:
            cpu: 1
            memory: 1Gi