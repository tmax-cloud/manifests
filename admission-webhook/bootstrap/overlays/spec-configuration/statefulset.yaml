apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: stateful-set
spec:
  template:
    spec:
      containers:
      - name: bootstrap
        resources:
          requests:
            cpu: 1  
            memory: 2Gi
          limits:
            cpu: 1
            memory: 4Gi