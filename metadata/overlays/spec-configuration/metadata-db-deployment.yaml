apiVersion: apps/v1
kind: Deployment
metadata:
  name: metadata-db
spec:
  template:
    spec:
      containers:
      - name: db-container
        resources:
          requests:
            cpu: 100m
            memory: 2Gi
          limits:
            cpu: 1
            memory: 4Gi